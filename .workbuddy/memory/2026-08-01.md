# 2026-08-01

## v6 大改 — 6项改造需求全部完成

用户提出6项改造需求，全部实现并部署。

### 1. 全局搜索筛选bug修复
- **根因**：所有带搜索栏的页面（客户资料/产品库存/客片管理/拍摄计划）的`oninput="refreshView()"`触发整个view重新渲染，input被销毁重建导致无法输入
- **修复**：引入全局`searchVals`对象持久化搜索值，渲染时设置input的value属性，渲染后调用`restoreSearchFocus()`恢复焦点和光标位置

### 2. 收支记录饼图重做
- 旧`pieChart()`替换为`pieChartFloat(items,title,mode)`：CSS `filter:drop-shadow()`浮动阴影 + hover上浮效果
- 去掉饼图上的收入/支出切换按钮
- 点击饼图扇区可直接筛选列表（`finPieSlice`变量记录选中扇区）
- 切换全部/收入/支出时饼图联动更新（全部=收支占比饼图，收入=收入分类饼图，支出=支出分类饼图）

### 3. AI话术助手改为可编辑模板库
- `AI_SCENARIOS`替换为`AI_TEMPLATES`：6个美妆场景模板（新品推荐/预约提醒/售后回访/活动推广/肤质咨询/价格沟通）
- 内容从美容改为美妆
- 每个模板有可编辑textarea + 复制按钮，`updateTemplate()`实时更新

### 4. 脚本标题从选题库匹配
- 使用`<datalist id="topicList">` + `<input list="topicList">`实现输入查找匹配选择

### 5. 脚本库重构为脚本管理
- 合并旧`shootDrafts`+`scripts`为统一`scripts`数组，每个脚本含`storyboard`字段
- 统一列表展示（标题/类型/日期/状态）
- 点击脚本名称→进入分镜拍摄编辑页（`enterStoryboard()`/`exitStoryboard()`）
- 点击编辑→编辑外层表单
- 拍摄完成的脚本显示"一键发布"按钮→转入发布管理（`scriptPublish()`）
- "内容日历"重命名为"发布管理"
- 数据迁移逻辑在`loadData()`中自动处理

### 6. 首页发布状态看板 + 发布管理双下拉筛选
- 首页新增4格发布统计（计划中/创作中/今日已发/今日计划）+ 今日发布内容列表
- 发布管理改为两个下拉筛选（平台+状态），默认全部

### 技术细节
- STORAGE_KEY从v4升级到v5确保干净迁移
- sw.js升级为network-first策略 + cache v2，确保用户获取最新版本
- 用户询问CloudStudio链接是否会失效，提议部署到GitHub Pages
- 安装了GitHub CLI (gh)，通过浏览器授权流程完成登录（用户账号 long0909）
- 创建仓库 long0909/luosha-beauty-workbench，推送代码
- 启用GitHub Pages（master分支根目录），状态变为built
- 永久部署地址: https://long0909.github.io/luosha-beauty-workbench/
- 更新项目MEMORY.md记录永久部署链接

## v7 改造 — 5项问题修复

用户反馈5个问题，全部修复并部署。

### 1. 顾客档期表单修复
- 去掉重复的两个客户名称字段（select+input），改为单个input+datalist匹配
- 必填项标注红色星号（客户姓名/日期/时间/服务项目）
- 空值逐项提示

### 2. 电脑端中文输入修复
- 根因：中文输入法IME组词时oninput频繁触发→refreshView重渲染→input被销毁
- 修复：添加isComposing标志+compositionstart/compositionend事件处理
- 4个搜索页面全部添加composition事件

### 3. 选题库标题同步修复
- datalist的option添加text content（之前只有value属性）

### 4. 分镜拍摄改为卡片式布局
- 旧：横向表格min-width:900px，不方便填写
- 新：卡片式布局，每个镜头一张卡片，字段竖向排列
- 手机端单列，桌面端（≥768px）三列网格
- 添加桌面端响应式CSS（body max-width:900px等）

### 5. 脚本状态外层编辑+发布同步+筛选标签
- 脚本列表页直接显示状态下拉（不用进编辑表单）
- 新增scriptSetStatus()：改状态为"已发布"时自动同步到发布管理
- 发布管理两个筛选下拉前加上"平台"和"状态"标签
- sw.js cache升级到v3

### 部署
- GitHub Pages: https://long0909.github.io/luosha-beauty-workbench/
- CloudStudio: https://6736465c3ec046fb8a39c1283cbff847.bj2.agentos-app.net

